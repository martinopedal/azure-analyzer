# Hunter sweep — all PR failures cleared

**Agent:** Hunter (one-shot diagnostic + fix)
**Worktree:** `C:\git\worktrees\hunter-20260422-234434` from `origin/main` @ `8d5aacc`
**Window:** 2026-04-22T23:44Z → 2026-04-22T23:55Z

## Headline

**Total failures found: 3** across 8 open PRs.
**All 3 fixed at root.** Zero failing checks remain on any open PR after sweep.

## Failure inventory

| PR | Job | Root cause | Fix |
|---|---|---|---|
| #466 | `Test (macos-latest)` | `[-] warns loudly when user explicitly requested trivy and it is missing` — pre-existing macOS Pester regression (`Write-Warning` capture inconsistent on POSIX hosts when caller lacks `[CmdletBinding()]`); same hit Atlas reported on #489 | Cascaded fix: PR #496 already had the cross-platform sourcing-contract refactor. Unblocked it (see below); after merge, every downstream PR auto-rebased and went green |
| #481 | `Markdown Link Check` (lychee) | `Cannot find file: docs/.../.squad/decisions/inbox/lead-8h-close-plan-2026-04-22.md` — the design doc referenced an inbox file that was never committed to the repo (close plan lived only in PR description) | Removed the broken local file link, replaced both occurrences with prose pointing at PR #481 itself. Rebased on `origin/main` (CHANGELOG conflict resolved by merging both Added entries). Pushed `--force-with-lease` |
| #496 | `Docs Check` | Test-only PR; gate requires a doc update | Added `### Fixed` CHANGELOG entry describing the cross-platform `MissingTool` test refactor. PR went green and was merged into `main` (commit `164d21b`) |

### Root-cause categorisation

| Bucket | Count | Notes |
|---|---|---|
| macOS-only Pester (Write-Warning capture) | 1 | Resolved at root by #496; cascaded to #466 / #494 / #495 / #498 via auto-rebase |
| Broken local markdown link | 1 | #481 |
| Docs-Check missing CHANGELOG | 1 | #496 — the very PR that fixed bucket 1; chicken-and-egg unblocked by the CHANGELOG add |

No `markdown-link-check` URL flake, no rerun-failed-checks self-loop, no third-party 5xx — all three failures had a code-resident root cause.

## PRs touched

| PR | Action | Result |
|---|---|---|
| #496 | Pushed `docs(changelog): note cross-platform MissingTool test fix (#496)` to `fix/missing-tool-test-cross-platform-472` | **Merged into main as `164d21b`** by the maintainer immediately after push (no admin toggle needed — required check turned green) |
| #481 | Pushed `fix(docs): remove broken inbox file ref in track-f design doc` and rebased onto current `origin/main` (CHANGELOG conflict resolved by retaining both Added entries) | All 16 required + advisory checks green; mergeable. Held for maintainer because it is a Track F **draft → ready** transition (Tier 4 work, gated until #466 merges per Tier rules) |
| #466 | No fix pushed | Now green (cascaded from #496 merge) and `MERGEABLE`, but **NOT merged** — has 13 unresolved Copilot review threads that require the 3-model rubberduck triage per the Cloud Agent PR Review contract. See **Remaining work** section below |
| #482 | No fix pushed | Still `CONFLICTING`; draft Tier 5 (ASCII banner). Per the mission rule "PR #482 absolute LAST", left untouched |
| #493 | No fix pushed | Was `CONFLICTING` at sweep start; auto-rebased to `MERGEABLE` after #496 merged into main. Verified non-duplicate of #483 — adds workflow-level `nick-fields/retry` wrapper and 4 new entries to the watchdog watchlist. All 17 checks green |
| #494 | No fix pushed | Was `UNKNOWN` at sweep start; now `MERGEABLE`, all 17 checks green |
| #495 | No fix pushed | Was `CONFLICTING`; now `MERGEABLE`, all 17 checks green |
| #498 | No fix pushed | Was `CONFLICTING`; now `MERGEABLE`, all 22 checks green |

### PRs closed as superseded
**None.** Specifically verified that #493 is **not** redundant against the already-merged #483 — #483 was a `.lychee.toml`-only fix; #493 adds a workflow-level retry wrapper and expands the CI failure watchdog to four additional gates. Complementary, not duplicative.

## Issues closed

| # | Reason |
|---|---|
| #497 | "fix: CI failure in Docs Check" on PR #496 — resolved by the CHANGELOG addition pushed during this sweep |
| #488, #485, #484, #476 | All four are `priority:p1 ci-failure` watchdog issues for `PR Auto-Resolve Review Threads` exit-code-1. Resolved by merged commit `fb7fa1f` (PR #487, "fix(ci): pr-auto-resolve-threads non-fatal on bot-vs-bot FORBIDDEN"). The workflow now logs FORBIDDEN as a warning instead of failing. Closed with link to the resolving commit |

**Total: 5 issues closed.**

Issues with `defer-post-window` (#491, #490, #462) and open feature/impl issues (#479, #478, #477, #433, #430) left untouched per their tracker labels.

## Remaining work — handed back to Conductor

1. **PR #466 Copilot triage backlog (13+ threads).** PR is green but not safe to merge until the Cloud Agent PR Review contract is honoured. The threads cluster into three real defects, not style nits:
   - **(D1)** README.md L44 + CHANGELOG.md L30 claim prompt/response credential sanitization for `modules/Invoke-CopilotTriage.ps1`, but the wired entrypoint is the legacy `Invoke-CopilotTriage.ps1` (not the new `modules/shared/Triage/Invoke-CopilotTriage.ps1`). Doc claims do not match shipping behaviour. **(High — user-visible accuracy + security claim)**
   - **(D2)** `config/triage-model-ranking.json` ships models forbidden by the frontier-only roster (`gpt-4.1`, `claude-sonnet-*`, `claude-haiku-*`, non-latest `*-codex`) and a placeholder `_header.sha256 = "PENDING-COMPUTED-AT-COMMIT"`. Same models also baked into the tier rosters in `Invoke-CopilotTriage.ps1`. **(High — policy violation per `.copilot/copilot-instructions.md` L37-42)**
   - **(D3)** `tests/triage/Triage.Tests.ps1` becomes non-deterministic if `AZURE_ANALYZER_COPILOT_TIER` is set in the runner env (fallback satisfies the throw assertion). Three test cases assert selection of the forbidden sonnet/haiku models — will require update once D2 is resolved. **(Medium — flaky CI risk)**

   I did **not** run the full 3-model rubberduck on #466 in this sweep — Hunter is one-shot and triaging 13 threads with `opus-4.7 + gpt-5.3-codex + goldeneye` 2-of-3 voting is a multi-turn task that belongs to the PR author or a follow-up Comment-Triage agent. Leaving the abbreviated verdict above as a starting brief.

2. **PR #482 (Tier 5 ASCII banner draft)** still `CONFLICTING`. Intentionally untouched per "PR #482 absolute LAST" rule.

3. **No `tech-debt` issues opened** — no `continue-on-error` band-aids or `-Skip` introductions during this sweep, so no debt was incurred.

## What did NOT need fixing (verified)

- `markdown-link-check` on every PR except #481 — `.lychee.toml` from #483 is doing its job; no flaky-URL strikeouts in this window.
- `rerun-failed-checks` workflow self-loop concern from the mission brief — confirmed not happening; #492 is concurrency-grouped per PR with `cancel-in-progress`, and the pull_request: synchronize trigger doesn't fire on its own commits.
- `Pester baseline` — preserved at 1666/1666/35-skipped on every PR's Linux + Windows runs (and macOS once #496 merged). No new failures introduced.

## Admin merge authority — not used

Did not exercise the enforce_admins toggle dance during this sweep. Both fixes (#496 CHANGELOG, #481 broken-link) were small enough that the standard required-check path went green within minutes; #496 was merged by the standard merge button, not `--admin`. The remaining mergeable PRs are blocked on review/triage, not on protection.

## Signed-off-by

Hunter sweep complete at 2026-04-22T23:55Z. CI is green. Ball back in Conductor's court for the #466 triage handoff.

— Hunter
