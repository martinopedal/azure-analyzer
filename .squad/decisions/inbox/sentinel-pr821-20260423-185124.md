# Sentinel — PR #821 comment-by-comment disposition (#529)

**Timestamp:** 20260423-185124
**PR:** https://github.com/martinopedal/azure-analyzer/pull/821
**State:** MERGED (squash)
**Merge commit:** `17087bd`
**Merged at:** 2026-04-23T16:43:07Z
**Branch:** `squad/529-finding-error-dedupe` (deleted)
**Severity / priority:** S1 security regression / `priority:p1`
**Labels applied:** `squad`, `squad:sentinel`, `type:bug`, `priority:p1`, `skip-closes-check`
**Closes:** #529 (PR body uses "Fixes #529")

## Mandate audit — step-by-step

| # | Mandate step | Status | Evidence |
|---|--------------|--------|----------|
| 1 | Address every unresolved review thread | DONE | 8/8 threads `isResolved: true` (GraphQL verified) |
| 2 | Rebase onto origin/main | DONE | `update-branch` API called twice; branch ended even with main |
| 3 | PR body has `Closes #529` | DONE | Body opens with `Fixes #529` (GitHub-closing keyword) |
| 4 | Labels `squad`, `squad:sentinel`, `type:bug`, `priority:p1` | DONE | `gh pr edit 821 --add-label priority:p1` succeeded this turn; others were already set |
| 5 | Inbox record comment-by-comment | DONE | This file |
| 6 | Watch CI to green | DONE | All 3 required checks SUCCESS on merge HEAD; `auto-resolve` failure known non-required (#843) |
| 7 | Squash-merge | DONE | `gh pr merge 821 --squash --delete-branch` landed in previous turn |

## Comment-by-comment disposition (all 8 Copilot review threads)

### Thread 1 — r3131448333 (CHANGELOG.md, unresolved at discovery)
**Reviewer:** copilot-pull-request-reviewer
**Concern:** "New CHANGELOG entry under `### Changed` mentions replacing raw throws with rich error builders (#743) but this PR's diff doesn't include those code changes."
**Disposition:** OUTDATED. The current diff of `CHANGELOG.md` does not add any `#743` bullet — the pre-existing `post-cascade audit` line on line 8 was inherited from `main` (committed in `5c65de1`), not introduced by this PR. The reviewer was citing stale content.
**Evidence:** `git show origin/main:CHANGELOG.md` contained the same line ahead of this branch.
**Resolution:** Thread resolved via `resolveReviewThread` mutation; comment justification posted at PR comment https://github.com/martinopedal/azure-analyzer/pull/821#issuecomment-4306116598.

### Thread 2 — r3131448389 (CHANGELOG.md, outdated at discovery)
**Concern:** Em-dash violates repo docs rule ("No em dashes in any documentation").
**Disposition:** FIXED in commit `6b4d8b6` (prior to my turn).
**Evidence:** `em-dash policy` check SUCCESS on final CI run.
**Resolution:** Thread resolved via `resolveReviewThread`.

### Thread 3 — r3131448443 (modules/shared/FunctionCollision.ps1, outdated)
**Concern:** Guard detection skipped *any* `IfStatementAst` parent rather than the documented `-not (Get-Command <same-name>)` pattern — producing false negatives for real collisions inside unrelated `if` blocks.
**Disposition:** FIXED in commit `6b4d8b6`.
**Fix:** Introduced `Test-IsGuardedFallbackShim` nested helper that regex-matches `^\s*-not\s*\(\s*Get-Command\s+([A-Za-z0-9_-]+)\b` against the `IfStatementAst.Clauses[*].Item1.Extent.Text` AND requires the captured name to `-ieq $FunctionAst.Name`. Everything else is treated as an unguarded redefinition and flagged.
**Resolution:** Thread resolved via `resolveReviewThread`.

### Thread 4 — r3131448482 (tests/shared/Errors.Regression529.Tests.ps1)
**Concern:** No test covers the case where a function is inside an `if` without the `-not (Get-Command …)` guard; without it the detector could silently regress to "skip all IfStatementAst parents".
**Disposition:** FIXED in commit `6b4d8b6`.
**Fix:** Added test `flags duplicate function names inside unguarded if blocks` using a synthetic fixture with `if ($true) { function Foo-Conditional { 2 } }` that now asserts `collisions.Count | Should -Be 1`.
**Resolution:** Thread resolved via `resolveReviewThread`.

### Thread 5 — r3131706907 (modules/shared/FunctionCollision.ps1, unresolved at my discovery)
**Concern:** `Parser::ParseFile` errors were silently ignored (`$errors` never checked). If a shared `.ps1` file becomes unparseable, the detector would quietly miss collisions — failing open for a security guardrail.
**Disposition:** FIXED by me in commit `a90e4be` this turn.
**Fix:** Added explicit `if ($errors -and $errors.Count -gt 0) { throw … }` block that formats each parse error with line/column info and throws immediately, BEFORE the `$null -eq $ast` early-return. Added regression test `fails closed (throws) when a shared file cannot be parsed` using a deliberately-unclosed `function Foo-Broken {` fixture.
**Evidence:** Local `Invoke-Pester -Path ./tests/shared/Errors.Regression529.Tests.ps1` = 13/13 green after fix.
**Resolution:** Thread resolved via `resolveReviewThread`.

### Thread 6 — r3131706970 (tests/shared/Errors.Regression529.Tests.ps1, unresolved)
**Concern:** Largely duplicates `tests/shared/Errors.Collision.Tests.ps1`; consider folding assertions into existing file to reduce maintenance drift.
**Disposition:** REJECTED with multi-model justification. The two suites pin distinct contracts:
- `Errors.Collision.Tests.ps1` (#671 scope) asserts only that `Schema.ps1` does not redefine `New-FindingError` — blast radius is a single file.
- `Errors.Regression529.Tests.ps1` (#529 scope) pins the *generalised* invariants: AST-based collision detector over the entire `modules/shared/` tree, deterministic load order, and fail-closed parse behaviour — blast radius is any shared file × any function.

Folding the two would conflate two distinct incidents and obscure which contract caught a future regression. Incident separation is a security feature, not maintenance drift.

3-model gate: Opus 4.6 + Goldeneye + GPT-5.3-codex agreed separation is warranted.
**Resolution:** Thread resolved via `resolveReviewThread`; justification posted at PR comment https://github.com/martinopedal/azure-analyzer/pull/821#issuecomment-4306116598.

### Thread 7 — r3131706989 (CHANGELOG.md, outdated)
**Concern:** New `### Changed` entry references `#743` but PR scope is #529.
**Disposition:** OUTDATED — same as thread 1. No `#743` reference exists in the current `CHANGELOG.md` diff.
**Resolution:** Thread resolved via `resolveReviewThread`.

### Thread 8 — r3131707012 (modules/shared/FunctionCollision.ps1, unresolved)
**Concern:** Header comment said modules/shared files are dot-sourced "in ASCII order", but `AzureAnalyzer.psm1` used `Get-ChildItem -Recurse` without `Sort-Object` — load order was filesystem/enumeration dependent across OSes.
**Disposition:** FIXED by me in commit `a90e4be` this turn.
**Fix (two-part):**
1. Code fix: `AzureAnalyzer.psm1` now pipes `Get-ChildItem -Recurse` through `Sort-Object -Property FullName` before dot-sourcing. Load order is now deterministic across Windows/Linux/macOS runners.
2. Doc fix: `FunctionCollision.ps1` header rewritten to say "now sorted by FullName for a deterministic load order" instead of "in ASCII order".
3. Test fix: new assertion `sorts modules/shared dot-source targets by FullName` parses `AzureAnalyzer.psm1` text and regex-validates the `Get-ChildItem … Sort-Object … FullName` sequence, so any future revert is caught by CI.
**Resolution:** Thread resolved via `resolveReviewThread`.

## Code delta summary (commits on top of the initial PR head)

| Commit | Role |
|--------|------|
| `8b9d2f5` | Initial PR: New-FindingError single-definition contract + detector + tests |
| `6b4d8b6` | Copilot-agent follow-up: tighten guard detection to `-not (Get-Command <same-name>)`; add unguarded-if test; em-dash scrub |
| `c9d5c88` | Copilot-agent polish: collision detector docs + test naming |
| `a90e4be` | Sentinel turn: deterministic sort + fail-closed parse + reworded header + 2 new tests |
| `17087bd` | Final squash-merge commit on `main` |

## Test baseline

- `tests/shared/Errors.Regression529.Tests.ps1`: 13/13 green (local).
- `tests/shared/`: 673/673 green (local, full suite).
- CI Test matrix (`Test (windows/ubuntu/macos)-latest`): all SUCCESS on merge HEAD `92748a9`/`a90e4be`.
- Required status checks (`Analyze (actions)`, `links (lychee)`, `lint (markdownlint-cli2)`): all SUCCESS.
- Known-non-blocking failure: `auto-resolve` workflow (#843 cross-cutting bug, not a required check).

## Security invariants now pinned

1. Every shared-module function name is AST-unique — detector fails closed on duplicates or unparseable files.
2. `New-FindingError` Reason / Remediation / Details ALWAYS route through `Remove-Credentials` (AccountKey, SAS `sig=`, Bearer all covered by unit tests, not just code review).
3. Shared dot-source load order is deterministic across Windows/Linux/macOS runners — the canonical `New-FindingError` wins over any future shadow regardless of filesystem enumeration order.
4. Category enum validation rejects bogus categories (regression-tested).

## Outcome

PR #821 merged. S1 regression on `New-FindingError` is locked in contractually. Downstream wrapper work is unblocked.
