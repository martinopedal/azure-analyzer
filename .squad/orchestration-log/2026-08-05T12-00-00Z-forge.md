# Orchestration Log: Forge (Windows Runners)

**Timestamp:** 2026-08-05T12:00:00Z  
**Agent:** Forge (Platform Automation & DevOps)  
**Task Summary:** Move `windows-latest` runners to GitHub-hosted pool, dropping dead `public-win` self-hosted runner dispatch branch.  

| Field | Value |
|-------|-------|
| **Agent routed** | Forge (Platform Automation & DevOps) |
| **Why chosen** | Issue #1173: dead `public-win` self-hosted Windows runner pool blocking all Windows CI |
| **Mode** | `background` |
| **Why this mode** | Standalone workflow runner infrastructure fix with no blocking dependencies |
| **Files authorized to read** | `.github/workflows/ci.yml`, `.github/workflows/e2e.yml`, `.github/workflows/release.yml`, `docs/operations/runners.md` |
| **File(s) agent must produce** | `.github/workflows/ci.yml`, `.github/workflows/e2e.yml`, `.github/workflows/release.yml`, `docs/operations/runners.md`, `CHANGELOG.md` |
| **Outcome** | Completed (PR #1250 merged at commit `e09ad611`) |

---

## Execution Details

- **Task:** Drop the `public-win` dispatch branch in `.github/workflows/` so `windows-latest` falls through to `|| matrix.os` on GitHub-hosted runners (free and unlimited for public repos). Keep Linux jobs on the healthy `public-linux` ACA pool.
- **Validation:** Issue #1173 auto-closed. `Test (windows-latest)` ran green in 5m40s and `e2e (windows-latest)` ran green in 1m20s - first successful Windows validation in two months. Zero accumulated Windows-only breakage found.
