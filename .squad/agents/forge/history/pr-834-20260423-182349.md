# PR #834 — Markdown Check lychee stabilization

**Status:** MERGED (squash) by martinopedal at 2026-04-23T16:22:49Z
**Merge commit:** 5c65de1199762be36e08d5a991faf054fed289d1
**Title:** fix(ci): stabilize Markdown Check lychee with PR-scoped link validation
**Tracks:** issue #832

## Outcome
- Arrived to find PR already merged; no driving work required.
- Required check `Analyze (actions)` green. `links (lychee)` green (11s). All hygiene gates (em-dash, markdownlint, docs update, redirect stub, closes/fixes, tool catalog, permissions, SBOM, advisory, rubberduck) passed.
- Matrix `Test (*)` / `LiveTool wrappers` / `Verify repro` were pending/non-blocking — not required for merge.

## Decisions / contract preserved
- `markdown-link-check.yml` remains exempt from global retry policy (per `tests/workflows/RetryWrapping.Tests.ps1`). Stability achieved via lychee-native retries + PR-scoped link diff, not by wrapping in the generic retry harness.
- Network flake handling: jittered retries inside lychee config (not blind suppression).

## Hygiene
- CHANGELOG + SHA-pinning assumed in-place as part of the merged diff (not re-audited since PR is closed).

## Cleanup
- Removed local worktree `C:/git/aa-pr834` and branch `pr-834`.
