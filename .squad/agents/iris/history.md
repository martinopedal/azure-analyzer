# Project Context

- **Owner:** martinopedal
- **Project:** ALZ Additional Graph Queries — Microsoft Graph/Entra ID checks for ALZ identity items
- **Stack:** PowerShell (Microsoft.Graph module), Microsoft Graph REST API, JSON
- **Created:** 2026-04-14

## Learnings

<!-- Append new learnings below. Each entry is something lasting about the project. -->
- 2026-04-21 — IaCFile EntityType (schema + EntityStore dedup contract) now available for IaC normalizers to use. Canonical ID: `iacfile:{repo-slug}:{relative-path}` (e.g., `iacfile:github.com/org/repo:terraform/main.tf`). Enables cross-tool file-level dedup when terraform-iac, Trivy, Checkov, and tfsec report findings on the same IaC file. Migration of terraform-iac normalizer from EntityType=Repository to IaCFile deferred to follow-up. PR #423 (SHA 5577bd77).

- 2026-04-22 — Maester `ConvertTo-MtMaesterResult` is the canonical structure-of-truth for what the wrapper SHOULD capture. Key fields the wrapper currently drops: `Tag` (Block.Tag + Test.Tag — drives compliance-framework badges like EIDSCA/CIS/MITRE/eIDAS2/NIST), `ResultDetail.TestDescription`/`TestResult`/`TestRemediation` (all markdown — must be rendered as HTML, not flattened), `HelpUrl` parsed from `See https://...` in test name, `Investigate` and `Error` result states (both squashed to Compliant=true today), `Duration`, `ScriptBlock`/`ScriptBlockFile`, Maester module version. Severity comes from `ResultDetail.Severity` (set by `Add-MtTestResultDetail -Severity` or `Get-MtMaesterConfigTestSetting`), with values Critical|High|Medium|Moderate|Low|Info — our existing word-boundary regex in `Normalize-Maester.ps1` already handles `moderate→Medium` correctly.
- 2026-04-22 — Maester native HTML report (4 MB single-file React-style bundle in `powershell/assets/ReportTemplate.html`) IA: branded header → 6-tile counter strip (Total/Passed/Failed/Investigate/Skipped/NotRun, doubles as filter chips) → per-block summary table → expandable test rows. Each row split as `TestId: Title` (e.g. `EIDSCA.AF01: Authentication Method – FIDO2 – State`) with severity pill, tag chips, and on-expand Description+Evidence+Remediation panels (all markdown). Includes "About this run" panel with `MgContext`, `SystemInfo`, `PesterConfiguration` for auditor reproducibility. Decision dropped at `.squad/decisions/inbox/iris-maester-ui-patterns.md` for Sentinel.
- 2026-04-22 — Kubescape v2 JSON (`--format json --format-version v2`) carries `summaryDetails.score` (cluster posture 0–100, hero KPI), `summaryDetails.complianceScore`, `summaryDetails.frameworks[]` (per-framework score), `summaryDetails.statusCounters` (passed/failed/skipped/**excluded**/**irrelevant** — 5 buckets, not 3), and per-control `scoreFactor` (1–10 → our Critical/High/Medium/Low bands at ≥9 / ≥7 / ≥4 / else). Per-resource details live in `results[].controls[].rules[]` with `paths` (failed JSONPath) and `fixPaths` (assisted-remediation YAML). Frameworks observed: `NSA`, `MITRE`, `cis-v1.10.0`, `cis-v1.23-t1.0.1`, `cis-aks-t1.2.0`, `cis-eks-t1.7.0`, `ArmoBest`, `AllControls`, `WorkloadScan`, `DevOpsBest`, `soc2`, `security`, `ClusterScan`. Our wrapper drops ALL of: framework membership, control description, real remediation text, fixPaths, scoreFactor, posture score, statusCounters, and per-resource granularity (we collapse N affected pods into 1 cluster-level finding). Recommendation to Sentinel: unify `<framework-chip>` component across Maester+Kubescape (same colors, framework-keyed not tool-keyed, so CIS is always amber regardless of source); keep posture score and severity enum as separate KPIs (different questions); emit one FindingRow per `(controlID, resourceID)` pair with cluster as `EntityId` and K8s coordinates in `SubResourceId`. Decision dropped at `.squad/decisions/inbox/iris-kubescape-ui-patterns.md`.
- 2026-04-22 — ETL gap remediation pattern (codified in both Maester and Kubescape inbox drops under `## ETL gaps end-to-end (revised)`): every dropped tool field needs a fix at all 5 layers — wrapper → normalizer → schema → entity store → report. Schema v2.1 already provisions `Frameworks: object[]`, `Controls: string[]`, `RuleId`, `Confidence`, `MissingDimensions`, `LearnMoreUrl`, `Remediation`, `Category` on `FindingRow` (`Schema.ps1:206–231`); most "dropped" fields just need wrappers/normalizers to populate them. Three shared schema additions needed for both tools (land once): (1) `New-FindingRow -Properties [object]` open container (bump to v2.2, additive, back-compat); (2) entity-store envelope v3.1 → v3.2 with `RunContexts: object[]` sidecar for per-tool/per-run scan metadata (posture scores, status counters, MgContext, tool versions); (3) shared `modules\shared\Frameworks.ps1` with `Get-FrameworkFromTag` classifier so EIDSCA/CIS/MITRE/NSA tags from any tool render with identical chip styling. First-class vs. container rule of thumb: cross-tool concepts and queryable identifiers go first-class (Frameworks, RuleId, LearnMoreUrl, Remediation); long markdown bodies, tool-specific numerics, and audit payloads go in `Properties`; per-tool/per-run metadata (tool version, scan context, cluster posture score) goes in `RunContexts` (envelope sidecar, not on every row). Per-resource granularity (Kubescape SubResourceId) deferred from EntityType promotion until we design cluster→namespace→workload Edges.
- 2026-04-22 — **Report UX arc complete.** Briefs merged to `decisions.md`. Schema 2.2 contract locked (13 new optional fields). Maester ETL fix tracked as #305, Kubescape ETL as #306. Framework badge palette ratified (CIS=amber, MITRE=red, EIDSCA=blue). Unified `<framework-chip>` component adopted (framework-keyed colors, not tool-keyed).
- 2026-04-22 — **v2 HTML report generator foundations shipped (PR1 of 3).** Refactored `New-HtmlReport.ps1` with v2 design system per Sage's spec (`.squad/decisions/inbox/sage-report-ui-v2-redesign.md`): CSS custom properties for light+dark mode (WCAG AA), system fonts (no CDN), semantic landmarks, skip-to-content link, dark mode toggle (localStorage + prefers-color-scheme), severity icon decorators (color-blind safe), pillar bars, filter chips, responsive grid (>=360px mobile), print stylesheet, prefers-reduced-motion. Extended test suite 8→15 cases. Regenerated `samples/sample-report.html`. Deferred MITRE 12-col heatmap, Impact×Effort matrix, framework table, entity pivot to PR2/PR3.
- 2026-04-21 — Added Invoke-AzureQuotaReports.ps1 wrapper for issue #322. Wrapper fans out across subscriptions and regions using Azure CLI (az vm list-usage, az network list-usages) with retry+timeout, emits v1 findings, supports -Subscriptions/-Locations scope controls, and uses compliant = UsagePercent < Threshold (default 80) with Reliability/Capacity metadata.
- 2026-04-22 — Added Normalize-AzureQuotaReports.ps1 for issue #323. The normalizer emits one FindingRow per wrapper record, canonicalizes Subscription IDs via ConvertTo-CanonicalEntityId, locks compliant to UsagePercent < Threshold (default 80), and maps severity using the quota ladder (Critical >=99, High >=95, Medium >=threshold, Info below).
- 2026-04-22 — Deepened `Invoke-AzureQuotaReports` wrapper tests (#324) with realistic Azure CLI fixtures (`tests/fixtures/azure-quota/cli/*`) covering subscription/region fanout, scope filters, threshold passthrough, sanitization, and v1 envelope shape. While authoring tests, found and fixed a retry bug: non-zero az exit codes were not thrown inside `Invoke-WithRetry`, so transient failures skipped retry; wrapper now retries and still emits sanitized installer-style failures for permanent errors.
- 2026-04-22 — Aligned `New-HtmlReport.ps1` to `samples/sample-report.html` for issue #295: sticky header/subnav single-scroll layout, provider/scope tool coverage tiles (manifest-driven), heatmap mode fallback to Tool×Severity when subscriptions are absent, top risks capped at 10 with findings anchors, and server-rendered findings `<tr>` rows with conditional Schema 2.2 detail blocks.
- 2026-04-21 — Post-#418 sample cleanup (PR #421). Confirmed `New-HtmlReport.ps1` and `New-MdReport.ps1` belong at the repo root: they are public module entry-points exported by `AzureAnalyzer.psd1`, wrapped by `AzureAnalyzer.psm1`, and invoked by `Invoke-AzureAnalyzer.ps1` via `$PSScriptRoot`. `scripts/` is for build/tooling like `Generate-ToolCatalog.ps1`, never for the report generators. Lesson: file mtime is not a proof of staleness — `sample-report.md` looked stale (older mtime than the HTML) but regenerating produced a byte-identical file, so the content was already aligned with `sample-findings-v2.json`. The actual drift was on the HTML side: the previously checked-in `sample-report.html` had been built from a smaller 5-finding fixture, not the curated 11-finding v2 dataset, so PR #418's regen step had used the wrong input. Always diff content, not timestamps, before declaring a sample stale.

## Decisions logged

- 2026-04-21 - v2 HTML report PR1 foundations (Maester/Kubescape UI patterns, framework badge palette, dark mode + accessibility) + ado-repos-secrets schema 2.2 ETL - to decisions.md section ## 2026-04-21 -- Post-#418 inbox sweep

